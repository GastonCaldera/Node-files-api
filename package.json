{
  "name": "api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "docker-build": "docker build -t node-restapi .",
    "docker-compose": "docker-compose -f docker-compose.yml up -d --build",
    "docker-compose-local": "docker-compose -f docker-compose.local.yml up -d --build",
    "start": "node src/app.js",
    "dev-docker": "nodemon -L src/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.27.2",
    "dotenv": "^16.0.1",
    "express": "^4.18.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.16"
  }
}
